## Code Listings

```swift
let url = URL(string: <#string#>)!
let value = <#[optional value]#>
```

```objc
NSURL *url = [NSURL URLWithString:<#string#>];
```

```terminal
$ swift build
Compiling…
```

```xcconfig
SWIFT_VERSION = 5.0 // comment
OTHER_SWIFT_FLAGS[config=Debug] = $(inherited) -DDEBUG
```

```jwt
eyJhbGciOiJFUzI1NiJ9.eyJpc3MiOiJ4In0.c2lnbmF0dXJl
```

Use `<#...#>` in `someLongMethodName:withArgument:` for a placeholder.

```
Plain text <b>not bold</b>
```

## Code Listings

A repeated heading.
